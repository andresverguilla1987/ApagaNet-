# ApagaNet Backend — .env.sample (Postgres + Notifs)
PORT=3000
JWT_SECRET=change_this
ALLOWED_ORIGINS=["https://tu-sitio.netlify.app","http://localhost:5173"]

# Postgres
PGHOST=localhost
PGPORT=5432
PGDATABASE=apaganet
PGUSER=apaganet
PGPASSWORD=password

# Notifications (opcionales)
NOTIFY_WEBHOOK_URL=
SLACK_WEBHOOK_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="ApagaNet <no-reply@tu-dominio>"
EMAIL_TO_DEFAULT=tu@correo.com
